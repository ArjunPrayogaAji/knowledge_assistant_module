# Used by docker compose (defaults are also embedded in docker-compose.yml)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ac_dev
PASSWORD_SALT=dev_salt_change_me
SESSION_SECRET=dev_session_secret_change_me
CORS_ORIGIN=http://localhost:3000
PORT=4000

# Google Gemini API key (used by the rag service for embeddings + LLM)
GOOGLE_API_KEY=your_google_api_key_here

# Used by web
NEXT_PUBLIC_API_URL=http://localhost:4000

# RAG service (set automatically in docker-compose; override for local dev without Docker)
QDRANT_URL=http://localhost:6333
RAG_SERVICE_URL=http://localhost:8000
